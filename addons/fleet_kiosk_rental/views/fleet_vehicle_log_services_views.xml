<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Rename the "Services" UI to "Rental History" and surface rental data -->
    <record id="fleet.fleet_vehicle_log_services_action" model="ir.actions.act_window">
        <field name="name">Rental History</field>
    </record>

    <!-- The left-nav menu item keeps its own stored name (captured as "Services"),
         so rename it explicitly. Use <record> (not <menuitem>) to write only the
         name and avoid resetting parent/action/groups. -->
    <record id="fleet.fleet_vehicle_log_services_menu" model="ir.ui.menu">
        <field name="name">Rental History</field>
        <field name="parent_id" ref="fleet.fleet_vehicles"/>
    </record>

    <!-- Stat button label on the vehicle form (all three service-activity states) -->
    <record id="fleet_vehicle_view_form_cars_history" model="ir.ui.view">
        <field name="name">fleet.vehicle.form.rental.history</field>
        <field name="model">fleet.vehicle</field>
        <field name="inherit_id" ref="fleet.fleet_vehicle_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@invisible=&quot;service_activity != 'none'&quot;]/field[@name='service_count']" position="attributes">
                <attribute name="string">Rental History</attribute>
            </xpath>
            <xpath expr="//button[@invisible=&quot;service_activity != 'overdue'&quot;]/field[@name='service_count']" position="attributes">
                <attribute name="string">Rental History</attribute>
            </xpath>
            <xpath expr="//button[@invisible=&quot;service_activity != 'today'&quot;]/field[@name='service_count']" position="attributes">
                <attribute name="string">Rental History</attribute>
            </xpath>
        </field>
    </record>

    <!-- Services list: rental columns -->
    <record id="fleet_vehicle_log_services_view_tree_rental" model="ir.ui.view">
        <field name="name">fleet.vehicle.log.services.list.rental</field>
        <field name="model">fleet.vehicle.log.services</field>
        <field name="inherit_id" ref="fleet.fleet_vehicle_log_services_view_tree"/>
        <field name="arch" type="xml">
            <list position="attributes">
                <attribute name="string">Rental History</attribute>
            </list>
            <xpath expr="//field[@name='purchaser_id']" position="after">
                <field name="employee_id" optional="show"/>
                <field name="rental_start" optional="show"/>
                <field name="rental_return" optional="show"/>
            </xpath>
        </field>
    </record>

    <!-- Services form: rental section -->
    <record id="fleet_vehicle_log_services_view_form_rental" model="ir.ui.view">
        <field name="name">fleet.vehicle.log.services.form.rental</field>
        <field name="model">fleet.vehicle.log.services</field>
        <field name="inherit_id" ref="fleet.fleet_vehicle_log_services_view_form"/>
        <field name="arch" type="xml">
            <form position="attributes">
                <attribute name="string">Rental History</attribute>
            </form>
            <xpath expr="//field[@name='purchaser_id']" position="after">
                <field name="is_rental"/>
                <field name="employee_id" invisible="not is_rental"/>
                <field name="rental_start" invisible="not is_rental"/>
                <field name="rental_return" invisible="not is_rental"/>
            </xpath>
            <xpath expr="//field[@name='notes']" position="before">
                <separator string="Project Numbers" invisible="not is_rental"/>
                <field name="project_number_ids" invisible="not is_rental">
                    <list editable="bottom">
                        <field name="name"/>
                    </list>
                </field>
            </xpath>
        </field>
    </record>

</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Record rule: Employees can view their own adjustments -->
        <record id="hr_hours_balance_adjustment_employee_rule" model="ir.rule">
            <field name="name">Employee can view own balance adjustments</field>
            <field name="model_id" ref="model_hr_hours_balance_adjustment"/>
            <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Record rule: HR users can view all adjustments and create them -->
        <record id="hr_hours_balance_adjustment_hr_user_rule" model="ir.rule">
            <field name="name">HR users can view and create all balance adjustments</field>
            <field name="model_id" ref="model_hr_hours_balance_adjustment"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('hr.group_hr_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Record rule: HR managers have full access -->
        <record id="hr_hours_balance_adjustment_manager_rule" model="ir.rule">
            <field name="name">HR managers have full access to balance adjustments</field>
            <field name="model_id" ref="model_hr_hours_balance_adjustment"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('hr.group_hr_manager'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>
</odoo>
